# PersonaRAG — consolidated service RUNTIME requirements.
# Kept deliberately lean: the deployed image needs nothing that pulls in PyTorch.
# The dense retriever uses ChromaDB's bundled ONNX MiniLM-L6-v2 (onnxruntime),
# so neither sentence-transformers nor bert-score belong here — they are
# build/eval-time only and live in requirements-dev.txt.
fastapi==0.136.1
uvicorn[standard]==0.47.0
pydantic==2.13.4
python-dotenv==1.2.2
google-genai>=2.2.0
chromadb==1.5.9
numpy>=2.4.5
pandas>=3.0.3
pyarrow>=24.0.0
# Optional alternative providers — uncomment if you switch LLM_PROVIDER
# anthropic==0.101.0
# openai==2.36.0
